# BOhub — Shell overrides

> Overrides `MASTER.md` for authenticated app shell only.
> **Brand tokens LOCKED** (do not replace with skill palettes):
> `--primary: #ccff00` · `--primary-foreground: #24292a` · `--background: #1a1d1e`

## Applied (PASO 4.1)

- Style spirit: Flat Design / professional dark dashboard (skill), colors = BOcode
- Skip link → `#main-content`
- Sticky header + backdrop blur (PH spirit)
- Nav density: `gap-2`, `h-9` hit targets
- Active: `bg-sidebar-accent` + `text-primary` (lime accent, not neon flood)
- Avatar muted (primary reserved for brand mark + active)
- Placeholders: clear h1 + short copy, no decorative glow cards
- Focus-visible rings with `ring-primary/40`
- Transitions 150–200ms; `cursor-pointer` on interactives
