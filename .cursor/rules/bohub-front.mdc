---
description: BOhub-front agent orientation — read AGENTS.md first
alwaysApply: true
---

# BOhub-front

Before coding, read **`AGENTS.md`**, then `docs/UI.md` / `docs/ARCHITECTURE.md` if needed.

- Routes under `/dashboard` (`App.tsx`)
- Lists: `ListPageShell` (see ClientsPage); don’t regress Home
- API via `src/lib/*.ts` + AbortSignal
- Tokens BOcode; buttons h-9 / primary lime
- No emails settings UI; no campaigns
- `pnpm run build` after UI changes
